version: 0.2

phases:
  install:
    commands:
      - yum install -y zip
  build:
    commands:
      - zip -r code.zip .
  post_build:
    commands:
      - echo dmompleted.......................
artifacts:
  files:
    - code.zip