version: 0.2

phases:
  install:
    commands:
      - sudo yum update -y
      - yum install -y zip
  pre_build:
    commands:
      - echo Build started on `date`
  build:
    commands:
      - cd code
      - zip -r ../code.zip *
artifacts:
  files:
    - code.zip