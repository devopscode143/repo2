version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.9
    commands:
      - pip3 install awscli
  build:
    commands:
      - git archive --format=zip HEAD > pr-codebase.zip
artifacts:
  type: S3
  files:
    - pr-codebase.zip