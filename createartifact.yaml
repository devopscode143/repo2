version: 0.2

phases:
  install:
    commands:
      - yum install -y zip
  build:
    commands:
      - cd $CODEBUILD_SRC_DIR
      - zip -r ../code.zip *
      - cd ..
  post_build:
    commands:
      - echo dmompleted.......................
artifacts:
  files:
    - code.zip